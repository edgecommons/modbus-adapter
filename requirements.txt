# edgecommons UNS core, pinned to the released tag @python-lib/v0.2.0 (the UNS core, merged to main).
# Never bump this pin alone; it moves together with a UNS-aware component. For local dev,
# `pip install -e ../core/libs/python` already satisfies this.
edgecommons @ git+https://github.com/edgecommons/edgecommons.git@python-lib/v0.2.0#subdirectory=libs/python
pymodbus>=3.6
